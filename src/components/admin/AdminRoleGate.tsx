"use client"

/**
 * DEPRECATED NO-OP: `AdminRoleGate` removed — authorization is enforced by
 * server-side `middleware.ts` (and by server APIs using `getSession()`).
 * This stub remains for compatibility with any stray imports; remove imports
 * to clean up code.
 */
export default function AdminRoleGate() {
  return null
}
